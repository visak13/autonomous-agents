---
name: e4b-long-report-citation-ceiling
description: "ReactiveAgents s9: the d49/d50 agentic flow works, but E4B can't reliably cite real URLs / honor depth on LONG deep-research reports — a measured model ceiling, not a code gap"
metadata: 
  node_type: memory
  type: project
  originSessionId: 2dfcdfd4-b783-4b27-a55e-f178ab1b45d9
---

ReactiveAgents s9 Stage-B (d46–d52) shipped + reviewer-confirmed: synthesizer reliability (raw read-back loop), route-independence, plan-chaining, selection off format=schema, multi-spec-per-node, 6→3 role collapse, MCP authoring tools, fetch-flag retirement, html styling. The 4 c6-matrix defects all FIXED+live (multi-round memory, full duplicate-document, .txt extension, csv-trailing-prose) + #2b strict-single-doc.

THE ONE UNMET BAR = the d47/o4 central gate "final report PRESERVES+CITES real sources" on the LONG concurrent-multi-node deep-research path. c12 implemented the CORRECT reasoning-driven fix (feed the real fetched URLs into the synthesizer prompt + cite-verbatim instruction; citations MODEL-authored, NOT code-injected — trace-verified per the neuron's d46/d14 mechanism ruling). But c12r measured **2/2 fresh US-Iran long runs cited ZERO of 88/62 real fetched URLs** (placeholder skeletons `URL 1`/`Source N Title` once, omitted+false-finished to a 4.4KB stub once); **depth also unstable (27.4KB vs 4.4KB same input)**.

ISOLATION proves it's the MODEL not the code: c8 SHORT demo PASS (3/3 real URLs cited verbatim on small clean input) + a pirate long run preserved 3 real links → E4B CAN cite when context is small/clean; it CHOKES building the ~18k–74k-token long multi-section HTML file-loop. Intermittent-not-uniform, but d47 demands RELIABLE → c12r REVISE-to-E4B-CEILING (d54), per the d53 watch-item.

KEY DISCIPLINE: per d53, do NOT patch-loop a model-variance ceiling and do NOT paper it with a citation template or code-injected Sources list (= the d46/d14 circumvention + a false-done). One focused attempt (c12), then surface the ceiling honestly → it's a MODEL/SCOPE decision for the user (d23/d24): (A) stronger base model for the long synthesis node, (B) bound/chunk the synthesis so the synth prompt stays in E4B's working regime, or (C) scope the long-sourced-report bar as best-effort. See [[gate-approval-needs-explicit-answer-not-recorded-finding]] — held s9 OPEN despite FSM=done/succeeded (a REVISE verdict recorded as action-done is NOT acceptance) pending the user's call.

**c13 UPDATE (s9, option-B realized = SPA-per-section bounded synthesis, d55/d56/d57; c13r REVISE-qualified).** The citation HALF of this ceiling is now SOLVED: per-section SOURCE-SCOPING (model assigns each write-node its own source_ids; runtime feeds ONLY that section's real sources at the prompt END via render_scoped_sources, inside the ~512-tok sliding window). c13r 2/2 fresh US-Iran live: **22/22 & 6/6 real URLs cited, 0 fabricated** (reverses the 0/88), depth substantive (34KB/12.5KB, no stub), SWA CAUSALLY proven (scoped block at rel_pos 0.79-0.99 = cursor). So the SWA citation ceiling was a BOUNDING problem, fixable by option-B WITHOUT a model swap — UPDATE the "can't reliably cite real URLs" framing: it CAN once each section's sources stay in-window.
BUT a DISTINCT residual blocked CONFIRM (still REVISE): E4B OVER-PRODUCES the long "detailed" report — (1) re-emits whole-report/section passes → **section duplication 4/4 long runs** (body-level `<header>/<nav>/<h2>` passes slip past enforce_single_html_document, which only dedups DOCTYPE/`<html>`) — a bounded d48-clean dedup/finish fix, MODEL-AGNOSTIC; (2) **fabricates provisions in UNSCOPED narrative/padding sections** (legal d58 run quoted a non-existent "17 USC §107(5) added by the 1998 CTEA") — scoping constrains fidelity ONLY where a section is scoped to real sources; the narrative is unconstrained = the genuine remaining E4B ceiling. Faithfulness 1/2 on the legal stress; the GROUNDED parts (cited factor text, real .gov URL verbatim ×16, 0 foreign URLs) were faithful. Recommend: land the dedup/finish fix + re-verify, and take narrative-depth-vs-fidelity (scope-the-bar C vs model-swap A) to the user — do NOT loop more citation-path patches (that lever works). Reviewer gotchas: faithfulness fabrication hid in the UNSCOPED narrative section (read those, not just scoped factor text); an EXACT-title dup detector misses near-dup sections (wording drift) — judge duplication by repeated `<h1>`/`<nav>`/heading-family, not string-equality. Access guard (distinct from fidelity): govinfo.gov reachable; ecfr.gov soft-blocks (200 + "aggressive automated scraping" notice); congress.gov 403; uscode.house.gov timeout.

**s16/RP-5a UPDATE (d360 forensic REFUTES the "model ceiling" framing).** On the s16 US-Iran served demo the long HTML report again showed ~33/41 fabricated cited URLs — but the 6-agent forensic (d356/d360) PROVED the research phase does NOT hallucinate: every sampled FACT traces VERBATIM to fetched tool output. So the fabricated URLs are NOT an E4B citation ceiling; they have TWO fixable NON-ceiling causes: (1) **POISONED SEARCH CORPUS** — DDGS returned fabricated-publisher domains (militaryspend.org/thedefensenews.com/…) with NO source-provenance gate, so the pipeline FAITHFULLY grounded a bad corpus (fix = a provenance gate on web_ingest + an unverified-publisher tier that can't be sole backing + ≥2-source corroboration at note-acceptance); (2) **WRITE-PLUMBING (Bug C)** — the section writers were never fed the real [S#] source index and truncated at num_predict 4096, so they fabricated citations to fill the mandated Sources block (fix = feed the [S#] index to writer turns / planner authors source_ids + raise compile num_predict>4096 + force-bind research_read on citing writers). The single-document COLLAPSE (concatenated fragments + section duplication) is likewise the write-loop NON-CONVERGENCE (d356/d360), NOT a ceiling. KEEP the NO-CEILING framing (d186/d198): don't label corpus/plumbing bugs a model ceiling. The ENGINE anti-fab is intact (it grounded the poisoned corpus faithfully + emitted the malformed doc raw, fixing nothing = the anti-fab win visible). Fixes are the s16 RP-6d (plumbing) + RP-7a/7b (provenance+corroboration). Supersedes the "measured MODEL ceiling" read for the CITATION axis.
